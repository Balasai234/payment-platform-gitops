adminUser: admin

adminPassword: admin123

service:
  type: loadBalancer

persistence:
  enabled: true
  storageClassName: gp2
  size: 10Gi

resources:
  requests:
    cpu: 100m
    memory: 256Mi
  limits:
    cpu: 500m
    memory: 512Mi